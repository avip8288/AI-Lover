#框架
1:记忆
    - 近期性
    - 相关性
    - 重要性
2:反思
    - 先从
3:规划

#语音互动
1:文本到语音
    - 选择调用11labs来进行TTS的能力 

#工具调用
- MCP server的实现，能够实现调用工具的能力
- 初步可以先实现函数调用，豆包大模型的函数调用可以看agent_practice中的32部分，看透这部分代码就可以开发一个函数调用
- 如果依然想用grok3-mini-beta，需要看下openrouter文档能否适配
- 不再使用LLM类构造大模型，直接用BaseChatModel来做

#prompt调整
- 更加的真人化，现在的

#搜索联网能力
- 实现tavily的搜索能力


#2025.5.12-5.18开发计划
- 1:向量存储和记忆优化
    - 对用户的文本进行切割，保证检索的时候精确度提升，避免长文本中的不相关部分导致检索效率低下（完成）
    - 对embedding模型输出的向量进行归一化，这样子计算向量相似性时就避免极值的影响（完成）
    - 对用户说的语料进行去重，主要做法是把这个语料直接做向量相似度匹配，把相似度得分超过阈值的部分，就舍去，不加入向量数据库；反之则加入。减少向量数据库的内存大小（完成）
    - 维护一个用户档案，变成一个json文件，里面的字段包括：名字，年龄，性别，喜好，讨厌的事等。可以后期不断增加，也可以在其中加一个抽取用户语料摘要的大模型，然后抽取出用户的信息填入到长期的用户档案json文件中去。先到用户档案里检索再从向量数据库里检索(完成用户档案的json建立，后续考虑通过大模型抽取，然后用eval送入到用户档案中去)

- 2:效果优化
    - 目前向量记忆管理不大行，时好时坏，需要调优的参数为:chunk_size,chunk_overlap,k
    - 每次进行测试时需删除向量数据库开始测试，然后需要比较归一化或者不归一化，也就是直接使用豆包embedding模型本身
- 3:用户档案检索技术流程
    - 搞个大模型抽用户语料的主要意思是什么，然后输入我们的json文件，之后开始检索


#2025.5.19-5.25开发计划
-1:搜索方向的实现（完成）
    - 实现tavily的搜索能力
    - 需要对问答类，联网的事情进行使用搜索，这个范围需要思考下，如何来做

-2 ：用langchain的chatopenai重构下代码(只修改了main.py)
-3 : 探索下实现文生图实现女友形象（目前看下来都好贵）
-4 : 效果优化，动态生成用户档案时，需要把联系历史也送进去让大模型去做摘要，不然单看一句语料会出现问题

#2025.5.26-6.1
1:开发AI情感状态
 -0 ：初始化一个AI情感状态
 -1 ：维护一个长期的messages的对话json文件，可以允许达到多少长度直接截断(完成)
 -2 ：接收用户的输入，输出用户的情绪和意图（用户的单轮对话）(完成)
 -3 ：根据messages,用户的情绪和意图，更新AI的情感状态
 -4 ：生成回复，输入更新后的AI情感状态，长期的messages,用户的情绪和意图，用户的输入语料，用户档案
 -5 ：保存AI情感状态

2:AI情感状态
 -1: 初始化一个AI情感状态，做成一个json文件，键是情绪，值是分数。长期保存
 -2: 分数想用一个MOE架构来做，但不知道应该怎么设计比较好
 -3: 情绪用一个LLM来做情绪分类就可以
 -4: 流程
    1. 用户输入 + 对话历史 → LLM → 用户情绪和意图（这里用单轮的来做就可以）
    2. 用户情绪和意图 + 对话历史 + 当前AI情感状态（json文件） → MOE → AI情感状态更新
    3. 更新后的AI情感状态 + 用户输入 + 对话历史 → 生成AI回复





